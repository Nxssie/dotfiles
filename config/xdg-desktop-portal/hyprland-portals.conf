# Portal backend selection for the Hyprland session (read via XDG_CURRENT_DESKTOP=Hyprland).
# hyprland: screencast/screenshot/global shortcuts (screen sharing in browsers, Discord...)
# gtk:      file chooser, settings (color-scheme, what browsers query for auto dark/light)
[preferred]
default=hyprland;gtk
org.freedesktop.impl.portal.FileChooser=gtk
