# Same SDK paths for processes started by systemd --user (apps launched from
# quickshell's launcher, IDEs); fish shells get them from conf.d/android.fish.
ANDROID_HOME=${HOME}/Android/Sdk
ANDROID_SDK_ROOT=${HOME}/Android/Sdk
PATH=${HOME}/Android/Sdk/platform-tools:${HOME}/Android/Sdk/cmdline-tools/latest/bin:${PATH}
