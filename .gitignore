# Real secrets — never commit (see conf.d/secrets.fish.example)
config/fish/conf.d/secrets.fish

# Quickshell runtime state (written on every theme toggle)
config/quickshell/theme_state.json

# Editor/OS noise
*.swp
.DS_Store

# Symlinks installed into ~/.config/fish by harnxss (shell/ is their source of truth)
config/fish/conf.d/ssh-agent.fish
config/fish/functions/ssh-unlock.fish
